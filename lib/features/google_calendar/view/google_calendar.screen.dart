import 'package:flutter/material.dart';

class GoogleCalendarScreen extends StatelessWidget {
  const GoogleCalendarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
